import argparse

parser = argparse.ArgumentParser(description='LASPI-Cert-Tool', prog="python3.10 CertTool")

# parser.add_argument('-m', '--module', type=str, help='Module to be tested')
# parser.add_argument('-p', '--pin', type=str, help='User PIN')
# parser.add_argument('-P', '--puk', type=str, help='User PUK')
# parser.add_argument('-n', '--name', type=str, help='Base name for the tokens')
# parser.add_argument('-r', '--requirements', type=str, help='Requirements to be tested')
# parser.add_argument('-s', '--slots', type=int, nargs='+', help='Slots to be tested')
